class Search{
    static binary (array, element){
        //your code here
        function find (array, element){
            let middle = Math.floor(array.length / 2);
            if (array[middle] == element){
                return array[middle]
            }
        }
    }
}